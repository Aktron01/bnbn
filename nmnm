--// OxyoHub Lite - Undetected Auto Parry & Spam //--
-- STREAMING_CHUNK:Initializing Core Services & Variables...
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local VirtualInputManager = game:GetService("VirtualInputManager")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local isMobile = UserInputService.TouchEnabled and not UserInputService.MouseEnabled

local AutoParryEnabled = false

-- STREAMING_CHUNK:Removing old UI elements to prevent overlap...
local old_ui = CoreGui:FindFirstChild("OxyoLiteUI")
if old_ui then
    old_ui:Destroy()
end

-- STREAMING_CHUNK:Creating the simplistic one-button Toggle UI...
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "OxyoLiteUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

local ToggleButton = Instance.new("TextButton")
ToggleButton.Name = "ToggleBtn"
ToggleButton.Size = UDim2.new(0, 130, 0, 45)
ToggleButton.Position = UDim2.new(0.5, -65, 0.1, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(150, 40, 40) -- Starts Red (OFF)
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.TextSize = 15
ToggleButton.Text = "OXYO: OFF"
ToggleButton.AutoButtonColor = true
ToggleButton.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = ToggleButton

local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(255, 255, 255)
UIStroke.Thickness = 2
UIStroke.Parent = ToggleButton

-- STREAMING_CHUNK:Configuring the UI Dragging Logic for both PC and Mobile...
local dragging, dragStart, startPos

ToggleButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = ToggleButton.Position
    end
end)

ToggleButton.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        ToggleButton.Position = UDim2.new(
            startPos.X.Scale, startPos.X.Offset + delta.X,
            startPos.Y.Scale, startPos.Y.Offset + delta.Y
        )
    end
end)

-- STREAMING_CHUNK:Defining the Toggle Button On/Off Logic...
ToggleButton.MouseButton1Click:Connect(function()
    AutoParryEnabled = not AutoParryEnabled
    if AutoParryEnabled then
        ToggleButton.BackgroundColor3 = Color3.fromRGB(40, 150, 40) -- Green
        ToggleButton.Text = "OXYO: ON"
    else
        ToggleButton.BackgroundColor3 = Color3.fromRGB(150, 40, 40) -- Red
        ToggleButton.Text = "OXYO: OFF"
    end
end)

-- STREAMING_CHUNK:Building the Stealth Input Mechanism (Bypasses AC)...
local lastClickTime = 0
local nextRandomDelay = 0.1

local function SafeParry()
    -- Dynamic CPS Limiter: Randomize the delay between clicks to mimic human inconsistency
    if tick() - lastClickTime < nextRandomDelay then return end
    lastClickTime = tick()
    nextRandomDelay = math.random(10, 16) / 100 -- Random delay between 0.10s and 0.16s

    pcall(function()
        if isMobile then
            -- Mobile: Tap realistic thumb coordinates (Bottom Right side of the screen)
            local cam = workspace.CurrentCamera
            local rx = cam.ViewportSize.X * 0.75 + math.random(-30, 30)
            local ry = cam.ViewportSize.Y * 0.75 + math.random(-30, 30)
            local holdTime = math.random(15, 35) / 1000

            VirtualInputManager:SendMouseButtonEvent(rx, ry, 0, true, game, 0)
            task.wait(holdTime)
            VirtualInputManager:SendMouseButtonEvent(rx, ry, 0, false, game, 0)
        else
            -- PC: Pressing 'F' (Default Parry Key) is much harder to flag than artificial mouse clicks
            local holdTime = math.random(15, 35) / 1000
            if keypress then
                keypress(0x46) -- 0x46 is the hex code for 'F'
                task.wait(holdTime)
                keyrelease(0x46)
            else
                VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.F, false, game)
                task.wait(holdTime)
                VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.F, false, game)
            end
        end
    end)
end

-- STREAMING_CHUNK:Defining Calculation Helpers for Ping and Ball Target...
local function GetBall()
    local balls = Workspace:FindFirstChild("Balls")
    if not balls then return nil end
    for _, ball in pairs(balls:GetChildren()) do
        if ball:GetAttribute("realBall") then
            return ball
        end
    end
    return nil
end

local function GetPing()
    local success, ping = pcall(function()
        return game:GetService('Stats').Network.ServerStatsItem['Data Ping']:GetValue()
    end)
    return success and ping or 50
end

local function GetClosestPlayerDist(character)
    local maxDist = math.huge
    local aliveFolder = Workspace:FindFirstChild("Alive")
    if not aliveFolder then return maxDist end

    for _, entity in pairs(aliveFolder:GetChildren()) do
        if entity ~= character and entity.PrimaryPart then
            local dist = (character.PrimaryPart.Position - entity.PrimaryPart.Position).Magnitude
            if dist < maxDist then
                maxDist = dist
            end
        end
    end
    return maxDist
end

-- STREAMING_CHUNK:Implementing the Main Combat and Humanization Loop...
RunService.PreSimulation:Connect(function()
    if not AutoParryEnabled then return end

    local char = LocalPlayer.Character
    if not char or not char.PrimaryPart then return end

    local ball = GetBall()
    if not ball then return end

    local zoomies = ball:FindFirstChild("zoomies")
    if not zoomies then return end

    -- Only check if the ball is targeting you
    if ball:GetAttribute("target") ~= tostring(LocalPlayer) then return end

    local velocity = zoomies.VectorVelocity
    local speed = velocity.Magnitude
    local distance = (char.PrimaryPart.Position - ball.Position).Magnitude

    local ping = GetPing()
    local pingAdjust = ping / 10

    -- [1] AUTO SPAM LOGIC (High Speed & Close Proximity)
    local closestPlayerDist = GetClosestPlayerDist(char)
    -- Add slight variance to spam distance threshold to avoid perfect mathematical triggers
    local spamHumanizer = math.random(-5, 5) / 10
    local spamDistanceThreshold = pingAdjust + math.min(speed / 3.5, 90) + spamHumanizer

    if distance < spamDistanceThreshold and speed > 40 and closestPlayerDist < 25 then
        SafeParry()
        return
    end

    -- [2] STANDARD AUTO PARRY LOGIC
    local cappedSpeedDifference = math.min(math.max(speed - 9.5, 0), 820)
    local speedDivisorBase = 2.4 + (cappedSpeedDifference * 0.002)
    
    -- CRITICAL HUMANIZATION: The offset ensures you don't parry at the exact same sub-millimeter distance every time
    local humanizeOffset = math.random(-2, 6) / 10
    local parryAccuracy = pingAdjust + math.max(speed / speedDivisorBase, 9.5) + humanizeOffset

    if distance <= parryAccuracy then
        SafeParry()
    end
end)
```eof
